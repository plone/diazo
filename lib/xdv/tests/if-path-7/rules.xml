<rules
    xmlns="http://namespaces.plone.org/xdv"
    xmlns:css="http://namespaces.plone.org/xdv+css"
    >
    <!-- an exact path match to the root -->
    <copy
        if-path="/"
        css:theme="#content"
        css:content="body > *"
        />
</rules>
