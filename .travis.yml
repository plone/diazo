language: python
python:
  - 2.6
  - 2.7
before_install:
install:
  - pip install --use-mirrors flake8
  - flake8 setup.py
  - flake8 lib
  - python bootstrap.py -d
  - bin/buildout -c travis.cfg -q
script:
  - bin/test -s diazo
